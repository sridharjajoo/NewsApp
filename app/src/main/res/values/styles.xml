<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.fragment_search" parent="AppTheme">
        <!-- Customize your theme here. -->
        <!-- This line changes the color of background in fragment_search. -->
        <item name="android:textcolorPrimary">@color/#3F51B5</item>
        <!-- This line changes the color of layout in fragment_search. -->
        <item name="android:textcolorPrimaryDark">@color/#303F9F</item>
        <!-- This line changes the color of text in fragment_search. -->
        <item name="android:textcolorAccent">@color/#FF4081</item>
        <!-- This line changes the color of search icon in fragment_search. -->
        <item name="android:textwhite">@color/#FFFFFF</item>
    </style>

</resources>
